name: Keep Hermes Alive
on:
  schedule:
    - cron: '*/20 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Hermes
        run: |
          curl -s -o /dev/null -w "%{http_code}" "${{ secrets.HERMES_TUNNEL_URL }}/ping"
